#include <iostream> // Includes the input/output library

int main() {
    std::cout << "Hello, World!" << std::endl; // Prints text and a new line
    return 0; // Signals to the OS that the program ran successfully
}